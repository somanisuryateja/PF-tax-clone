# Frontend (Vite + React)

## Environment configuration

Create a `.env.local` file in the project root with:

```
VITE_API_BASE_URL=http://192.168.0.11:5000
```

Adjust the host or port to match wherever the backend is running.

## Development

```bash
npm install
npm run dev
```
